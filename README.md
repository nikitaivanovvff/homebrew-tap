# Homebrew tap for gh-view

Install [`gh-view`](https://github.com/nikitaivanovvff/gh-view):

```sh
brew tap nikitaivanovvff/tap
brew install gh-view
```

`gh-view` uses the GitHub CLI for GitHub access. Authenticate `gh` before using live mode:

```sh
gh auth login
```
